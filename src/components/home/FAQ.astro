---
import { Plus, Minus } from "lucide-react";

const faqs = [
  {
    q: "¿Workover Web es 'reparar' mi sitio?",
    a: "No. Es una reconstrucción total sobre una base nueva. No parchamos código viejo; mantenemos tu contenido y diseño visual (si funciona), pero reconstruimos toda la ingeniería interna para que deje de ser frágil, lenta o inmantenible."
  },
  {
    q: "¿Cuánto tarda el proceso?",
    a: "Depende del alcance. Un 'Workover' típico suele ser un sprint rápido de 2 a 4 semanas. El modelo 'Modular' varía según los módulos elegidos. En ambos casos, definimos un DoD (Definition of Done) claro antes de empezar."
  },
  {
    q: "¿Me van a prometer ventas o ser #1 en Google?",
    a: "No. Huimos de las promesas de vanidad. Trabajamos con medición real, mejoras técnicas (Core Web Vitals), estructura semántica impecable y mensajes que convierten. Los resultados de venta dependen de tu mercado y tu oferta; nosotros te damos la mejor herramienta para competir."
  },
  {
    q: "¿Puedo mantener la web sin depender de la agencia?",
    a: "Sí, es el objetivo. Te entregamos una base ordenada y un panel de administración simplificado (CMS) para que puedas editar textos, imágenes y productos sin tocar código. Solo nos llamas para evolutivos o problemas complejos."
  },
  {
    q: "¿Incluye tracking y analítica?",
    a: "Sí. Implementamos un tracking mínimo útil (eventos clave como 'Click en WhatsApp' o 'Formulario Enviado'). No llenamos tu web de scripts basura que la ralentizan. Datos limpios para decisiones claras."
  },
];

// Generación de Schema.org para SEO
const schema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqs.map(f => ({
    "@type": "Question",
    "name": f.q,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": f.a
    }
  }))
};
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />

<section class="py-24 bg-black relative overflow-hidden" id="faq">
  
  <div class="max-w-4xl mx-auto px-6 relative z-10">
    
    <div class="text-center mb-16">
      <span class="inline-block py-1 px-3 rounded-full bg-white/5 border border-white/10 text-[10px] font-bold tracking-widest uppercase mb-4 text-gray-400">
        DUDAS FRECUENTES
      </span>
      <h2 class="text-3xl md:text-5xl font-black uppercase tracking-tight text-white mb-6">
        PREGUNTAS TÍPICAS. <br/> <span class="text-transparent bg-clip-text bg-gradient-to-r from-gray-200 to-gray-500">RESPUESTAS DIRECTAS.</span>
      </h2>
      <p class="text-gray-400 text-lg max-w-2xl mx-auto">
        Sin letra chica. Lo que te importa saber sobre cómo trabajamos y qué entregamos.
      </p>
    </div>

    <div class="flex flex-col gap-4">
      {faqs.map((f, i) => (
        <details class="group bg-[#050505] border border-white/10 rounded-2xl overflow-hidden transition-all duration-300 open:bg-white/5 open:border-white/20 hover:border-white/20">
          <summary class="flex items-center justify-between gap-4 p-6 cursor-pointer list-none select-none">
            <span class="text-lg font-bold text-gray-200 group-open:text-white transition-colors">
              {f.q}
            </span>
            <div class="relative w-8 h-8 flex items-center justify-center rounded-full bg-white/5 border border-white/10 text-gray-400 transition-colors group-hover:bg-white/10 group-open:bg-white text-white group-open:text-black">
              <Plus size={16} class="block group-open:hidden transition-transform duration-300" />
              <Minus size={16} class="hidden group-open:block transition-transform duration-300" />
            </div>
          </summary>
          
          <div class="px-6 pb-6 pt-0 text-gray-400 leading-relaxed border-t border-transparent group-open:border-white/10 group-open:pt-4 text-base animate-fadeIn">
            {f.a}
          </div>
        </details>
      ))}
    </div>

  </div>

  <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-white/5 via-black to-black pointer-events-none"></div>

</section>

<style>
  /* Animación suave al abrir */
  details[open] summary ~ * {
    animation: sweep .3s ease-in-out;
  }

  @keyframes sweep {
    0%    {opacity: 0; transform: translateY(-10px)}
    100%  {opacity: 1; transform: translateY(0)}
  }
  
  /* Ocultar triángulo default */
  summary::-webkit-details-marker {
    display: none;
  }
</style>