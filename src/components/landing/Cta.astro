---
const { class: className } = Astro.props;

// Tecnologías reales de tu perfil profesional
const techStack = [
  { label: "Frontend & Performance", value: "React • Astro • JS" },
  { label: "Inteligencia Artificial", value: "Gemini • Agentes IA" },
  { label: "Mobile & Backend", value: "Flutter • Supabase • SQL" },
  { label: "Infraestructura TI", value: "Servidores • Redes" }
];
---

<section id="cta-section" class:list={["relative bg-zinc-950 py-24 overflow-hidden border-t border-zinc-900", className]}>
  <div class="absolute inset-0 z-0">
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[500px] bg-lime-500/10 rounded-full blur-[120px] opacity-40 animate-pulse"></div>
  </div>

  <div class="px-8 mx-auto md:px-12 2xl:max-w-7xl relative z-10">
    <div class="flex flex-col items-center">
      
      <div class="cta-reveal opacity-0 translate-y-4">
        <div class="inline-flex items-center gap-3 px-4 py-2 rounded-full border border-zinc-800 bg-zinc-900/50 backdrop-blur-md">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-lime-400 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-lime-500"></span>
          </span>
          <span class="text-[10px] font-mono font-bold tracking-[0.3em] uppercase text-zinc-400">
            Disponible para Proyectos de Alto Impacto
          </span>
        </div>
      </div>

      <h2 class="cta-title mt-12 text-center text-5xl md:text-8xl lg:text-[10rem] font-black leading-[0.8] tracking-tighter uppercase italic opacity-0 translate-y-10">
        ¿LISTO PARA <br/>
        <span class="text-lime-400">EVOLUCIONAR?</span>
      </h2>

      <p class="cta-text mt-12 max-w-3xl text-center text-zinc-400 text-lg md:text-xl font-medium leading-relaxed opacity-0">
        Desde arquitecturas en <span class="text-white">Astro y React</span> hasta ecosistemas de <span class="text-white">IA con Gemini</span>. 
        Transformo complejidad técnica en herramientas de facturación real.
      </p>

      <div class="cta-button-container mt-16 opacity-0 scale-90">
        <a
          href="https://wa.me/56966877441"
          target="_blank"
          class="group relative inline-flex items-center justify-center px-12 py-6 lg:px-20 lg:py-8 overflow-hidden font-black text-black transition-all duration-300 bg-lime-400 rounded-full hover:bg-white hover:scale-105 active:scale-95 shadow-[0_0_50px_rgba(163,230,53,0.4)]"
        >
          <span class="relative z-10 text-xl lg:text-3xl tracking-tighter uppercase italic">INICIAR CONSULTORÍA →</span>
          <div class="absolute inset-0 flex h-full w-full justify-center [transform:skew(-12deg)_translateX(-100%)] group-hover:duration-1000 group-hover:[transform:skew(-12deg)_translateX(100%)]">
            <div class="relative h-full w-10 bg-white/50"></div>
          </div>
        </a>
      </div>

      <div class="mt-24 grid grid-cols-2 md:grid-cols-4 gap-8 w-full border-t border-zinc-900 pt-12">
        {techStack.map((item) => (
          <div class="flex flex-col items-center md:items-start text-center md:text-left">
            <span class="text-zinc-600 font-mono text-[10px] uppercase tracking-widest">{item.label}</span>
            <span class="text-white font-bold uppercase italic mt-1 tracking-tight text-sm md:text-base">{item.value}</span>
          </div>
        ))}
      </div>

    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  function initCTA() {
    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: "#cta-section",
        start: "top 70%",
      }
    });

    tl.to(".cta-reveal", { opacity: 1, y: 0, duration: 0.6 })
      .to(".cta-title", { opacity: 1, y: 0, duration: 0.8, ease: "expo.out" }, "-=0.3")
      .to(".cta-text", { opacity: 1, duration: 0.8 }, "-=0.4")
      .to(".cta-button-container", { 
        opacity: 1, 
        scale: 1, 
        duration: 1, 
        ease: "elastic.out(1, 0.7)" 
      }, "-=0.6");
  }

  document.addEventListener("astro:page-load", initCTA);
  initCTA();
</script>

<style>
  .cta-title, .cta-text, .cta-reveal, .cta-button-container {
    will-change: transform, opacity;
  }
  
  @media (max-width: 640px) {
    .cta-title {
      font-size: 3.5rem;
      line-height: 1;
    }
  }
</style>